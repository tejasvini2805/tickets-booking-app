apiVersion: v1
kind: Service
metadata:
  name: ticket-booking-service
spec:
  type: LoadBalancer
  selector:
    app: ticket-booking
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
